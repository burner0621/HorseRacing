1. Development mode
   
    Run these commands as follows.

    ```
    npm install

    npm run dev
    ```

2. Production mode
   
   Run these commands as follows.

   ```
    npm install
    npm run build

    npm install -g serve
    serve -S dist
    ```